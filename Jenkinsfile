pipeline {
  agent any 
  stages {
    stage("build docker image for the backend") {
      steps {
        script{

        }
      }
    }
      stage("build docker image for the frontend") {
      steps {
        script{

        }
      }
    }
  }
}
